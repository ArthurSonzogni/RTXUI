# `<ul>`, `<ol>` and `<li>`, including nesting, `start`/`reversed`, per-item `value`, and the list-style-type property

`<ul>`, `<ol>` and `<li>`, including nesting, `start`/`reversed`, per-item `value`, and the list-style-type property.

<ExampleTabs src="/wasm/rtxui_example_lists.js" :cols="80" :rows="24">
<template #source>

<<< @/../example/lists.cpp

</template>
</ExampleTabs>

---

* Source file: [`example/lists.cpp`](https://github.com/ArthurSonzogni/RTXUI/blob/main/example/lists.cpp)
* Guide: [Relevant Documentation](/html_reference)
* [← Back to Examples Index](/guide/examples)
